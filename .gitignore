# Hugo build output
/public/
/resources/_gen/
.hugo_build.lock
.agents/*

# Regenerated on every build (buildStats → Tailwind cachebuster)
hugo_stats.json

# Node
/node_modules/

# Cloudflare Worker. `.dev.vars` holds live secrets and must never be committed.
/workers/*/node_modules/
/workers/*/.wrangler/
.dev.vars

# AI coding agents. Repo is public; PRODUCT.md quotes job-search positioning,
# so it stays untracked too. The rest are pre-emptive tool entries.
.claude/
.impeccable/
PRODUCT.md
CLAUDE.md
AGENTS.md
.cursor/
.cursorrules
.windsurfrules
.clinerules
.roo/
.continue/
.codeium/
.aider*
.gemini/
.opencode/
.mcp.json
.github/copilot-instructions.md

# OS
.DS_Store
Thumbs.db
